# cspell-parsers

This repository is a pnpm monorepo for cspell parser packages.

## Workspace layout

- `packages/*` contains parser packages.
- `packages/parser-example` is the starter package.

## Commands

```sh
pnpm install
pnpm run build
pnpm test
pnpm run lint
pnpm run lint-ci
```
