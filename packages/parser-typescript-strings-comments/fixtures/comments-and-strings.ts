// running total
let total = 0; // trailing note

/* approximate */
const pi = 3.14;

/**
 * Adds two numbers together.
 */
function add(a: number, b: number): number {
  return a + b;
}

const url = "see http://example.com"; // not a real comment: "/* nope */"
const initial = 'A';
const quoteInside = "she said \"hi\" then left";
