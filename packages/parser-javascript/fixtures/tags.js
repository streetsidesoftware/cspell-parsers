// leading comment
/** doc comment */
class Greeter {
  sayHello() {
    return 'hello';
  }
}

const greeting = 'hello';
const doubleQuoted = "hello";
const msg = `hi ${userName} bye`;
const describe = (label) => label;
